# PHP Task Manager

A simple task manager project in PHP demonstrating **for loops** and **while loops**.

## Features
- Add tasks
- List tasks
- Web and CLI versions
- Uses `for` loops to iterate tasks
- Uses `while` loops for CLI menu

## How to Run Web Version
1. Run PHP built-in server:
```bash
php -S localhost:8000
